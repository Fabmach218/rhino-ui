import { Spinner } from '@rhino-ui/components/src/components/spinner/spinner';
import { BRAND_COLOR_NAMES } from '@rhino-ui/components/src/lib/tokens';
import { Box } from '@rhino-ui/components/src/components/box/box';

# Spinner

Spinners are used to communicate a loading state to the user.
They may be used in buttons, forms, containers, pages,
and anywhere else a loading state is necessary.

<Spinner />

## Props

## Variants

Set the color variations of the spinner

<Box display="block" childGap="md">
    {BRAND_COLOR_NAMES.map(color => (
        <Box direction="row" gap="sm">
            <Spinner variant={color} />
            <div>{color}</div>
        </Box>
    ))}
</Box>


## Sizes

Use the `size` prop on your Spinner based on what's a best fit for your layout

<Box direction="row" gap="md">
    <Box direction="row" gap="sm">
        <Spinner size="sm" />
        <div>sm</div>
    </Box>
    <Box direction="row" gap="sm">
        <Spinner size="md" />
        <div>md</div>
    </Box>
    <Box direction="row" gap="sm">
        <Spinner size="lg" />
        <div>lg</div>
    </Box>
    <Box direction="row" gap="sm">
        <Spinner size="xl" />
        <div>xl</div>
    </Box>
</Box>
